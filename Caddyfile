dafeapp.com {
    # Serve Django static files directly (collected by web container on startup)
    handle_path /static/* {
        root * /staticfiles
        file_server
    }

    # Proxy everything else (including WebSocket upgrades) to Daphne
    handle {
        reverse_proxy web:8000
    }

    # Compress responses
    encode gzip
}
